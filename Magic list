def generate_list(start, end, step):
  lst = []
  i = start
  if start == end:
    return lst
  else:
    if step > 0:
      while i < end:
        lst.append(i)
        i += step

    else:
      while i > end:
        lst.append(i)
        i += step
    return lst
